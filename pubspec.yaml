name: valo_magaza
description: "Kişisel VALORANT günlük mağazasını görüntüleyen Android uygulaması."
publish_to: 'none'

version: 1.4.0+6

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  http: ^1.6.0
  flutter_secure_storage: ^11.0.0
  url_launcher: ^6.3.2
  cached_network_image: ^3.4.1
  shared_preferences: ^2.5.5
  provider: ^6.1.5+1
  intl: ^0.20.3
  video_player: ^2.9.2
  flutter_local_notifications: ^22.3.0
  share_plus: ^13.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icon/valo_magaza_icon.png
